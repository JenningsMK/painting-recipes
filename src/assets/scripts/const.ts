/**
 * @Summary A list of all the readOnly values that are used in the app
 * @module constants
 */

/**
 * Names of custom events
 * @enum {string}
 */
export enum CustomEvents {
  menuButtonToggle = 'menu-button-toggle',
  animatedButton = 'animated-button',
  animatedMainNav = 'animated-nav',
}

/**
 * Represents CSS classes used in the application.
 * @enum {string}
 */
export enum Classes {
  noScroll = 'no-scroll',
  toggleOverLay = 'overlay--appear',
  toggleNavigation = 'navigation--open',
  animatedNavButton = 'menu-button--animated',
}
